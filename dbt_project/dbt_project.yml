name: fda_pipeline
version: "1.0.0"
config-version: 2

profile: fda_pipeline

model-paths: ["models"]
test-paths:  ["tests"]

models:
  fda_pipeline:
    staging:
      +schema:       staging
      +materialized: view
    mart:
      +schema:       mart
      +materialized: table